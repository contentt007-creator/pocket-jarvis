# Backend API URL (use local IP for dev, Railway URL for production)
# For Expo Go testing: http://192.168.X.X:5000/api  (your machine's local IP)
# For production builds: your Railway URL + /api
EXPO_PUBLIC_API_BASE_URL=https://your-backend-url.up.railway.app/api
